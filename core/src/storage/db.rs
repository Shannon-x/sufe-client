//! SQLite schema (orders, history, log retention).
//!
//! Empty until we ship the order/history feature in Phase 4. We keep the
//! module so its public name is stable and the migration directory has a
//! place to live (`core/migrations/`, added when `sqlx` is wired in).
